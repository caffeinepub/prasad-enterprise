{
  "kind": "build_request",
  "title": "Switch canonical site URL to prasadenterprise-epm and update all internal domain references",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-43",
      "text": "Update the app’s primary site URL configuration so the canonical public URL is exactly https://prasadenterprise-epm.caffeine.xyz/ (including updating `SITE_CONFIG.url` in `frontend/src/config/site.ts`).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "recent-messages"
        ],
        "quotes": [
          "https://prasadenterprise-epm.caffeine.xyz/   this url is live, change the url with this",
          "1 & 3"
        ]
      },
      "acceptanceCriteria": [
        "`frontend/src/config/site.ts` sets `SITE_CONFIG.url` to `https://prasadenterprise-epm.caffeine.xyz/` (with trailing slash) or a single normalized form used consistently throughout the app.",
        "No user-facing UI text, links intended to represent the site’s primary URL, or metadata derived from `SITE_CONFIG` displays `https://prasadenterprise.caffeine.xyz`, `https://www.prasadenterprise.caffeine.xyz`, or any draft domain."
      ]
    },
    {
      "id": "REQ-44",
      "text": "Update all internal links or any hardcoded absolute URLs throughout the frontend to use the https://prasadenterprise-epm.caffeine.xyz/ domain (where absolute URLs are used), and remove/replace any remaining references to the old domains `https://prasadenterprise.caffeine.xyz`, `https://www.prasadenterprise.caffeine.xyz`, and any draft URLs such as `https://significant-blush-1gq-draft.caffeine.xyz/`.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "recent-messages"
        ],
        "quotes": [
          "1 & 3"
        ]
      },
      "acceptanceCriteria": [
        "A repository-wide search for `prasadenterprise.caffeine.xyz`, `www.prasadenterprise.caffeine.xyz`, and `significant-blush-1gq-draft.caffeine.xyz` returns no remaining user-facing references (excluding historical comments if any, but prefer removing those too).",
        "Any location where an absolute URL is presented to users (e.g., in copy, share links, SEO/canonical fields if present, or external link targets) uses `https://prasadenterprise-epm.caffeine.xyz/`."
      ]
    },
    {
      "id": "REQ-45",
      "text": "Ensure the admin area remains reachable on the new domain and that any user-facing guidance or navigation that points to the admin area uses the new domain (domain only; do not change admin paths unless already required elsewhere).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "recent-messages"
        ],
        "quotes": [
          "How can i go to admin page for this website to access the form fill data?",
          "1 & 3"
        ]
      },
      "acceptanceCriteria": [
        "Visiting the admin route on the new domain works (e.g., `https://prasadenterprise-epm.caffeine.xyz/admin/...` according to the app’s current routing) without requiring draft/admin-preview URLs.",
        "No user-facing admin URL instructions (if present anywhere in the UI) reference the old domain or a `caffeineAdminToken=` preview URL."
      ]
    }
  ],
  "constraints": [
    "Do not edit any files under `frontend/src/hooks/useInternetIdentity.ts`, `frontend/src/hooks/useInternetIdentity.tsx`, `frontend/src/hooks/useActor.ts`, `frontend/src/main.tsx`, or anything inside `frontend/src/components/ui` (compose them instead).",
    "Backend must remain single-actor; do not introduce additional backend services/canisters."
  ],
  "nonGoals": [
    "Do not change the app’s route structure or introduce new pages purely to handle this domain change.",
    "Do not attempt to configure DNS, TLS, or Caffeine platform publishing settings; only update application code/config so it consistently uses the new domain.",
    "Do not add third-party analytics, redirects, or external URL shorteners."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}